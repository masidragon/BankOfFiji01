@model IEnumerable<BankOfFiji01.Models.Transactions>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Trans_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trans_Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trans_Adjustment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bank_Account.Acc_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Transaction_Type.Transac_Type_Descript)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Trans_Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trans_Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trans_Adjustment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bank_Account.Acc_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Transaction_Type.Transac_Type_Descript)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Trans_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Trans_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Trans_ID })
        </td>
    </tr>
}

</table>
